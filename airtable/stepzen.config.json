{
  "name": "Airtable",
  "description": "Sample of a custom airtable schema using the Event Marketing Template base model.",
  "categories": ["jamstack"],
  "config": {
    "questions": [
      {
        "name": "airtable_config.Authorization",
        "message": "Add the api key for your airtable with the format - Bearer XXXX"
      },
      {
        "name": "airtable_config.table",
        "message": "What is the table id for your airtable?"
      }
    ]
  }
}
