{
    "config": {
      "questions": [
        {
          "name": "mailchimp_config.api_password",
          "message": "What is the authentication code for your mailchimp API?"
        }
      ]
    } 
  }