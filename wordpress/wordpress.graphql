interface Page {
    id: ID!
}
type WordpressPage implements Page {}
type Query {
    page (id: ID!): Page
    wordpressPage (id: ID!): WordpressPage
        @supplies (query: "page")
        @rest (
            resultroot: ""
            endpoint: 
            "http://wpsample.stepzen.com/wp-json/wp/v2/pages/$id?_embed"
            # Basic Auth - https://$username;:$password;@wpsample.stepzen.com/wp-json/wp/v2/pages/$id?_embed"
            configuration: "wordpress_config")
}