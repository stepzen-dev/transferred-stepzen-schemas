interface Location {
  city: String
  country: String!
  currency: String
  ip: String!
  latitude: Float!
  longitude: Float!
  postalCode: String
}

type Query {
  location(ip: String!): Location
}

## After importing weatherreport schema, uncomment
## the following three lines to extend the Location
## interface with the WeatherReport interface.
# extend interface Location {
#  weather: WeatherReport @materializer(query: "weatherReport")
#}
