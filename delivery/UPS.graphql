type UPS implements Delivery {}
type Query {
    ups (carrier: String!, trackingId: String!): FedEx
        @supplies (query: "delivery")
        @connector(type:"ups", configuration: "ups_default")
}