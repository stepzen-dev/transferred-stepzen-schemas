{
  "name": "FedEx",
  "description": "",
  "config": {
    "default": "fedex_default",
    "questions": [
      {
        "name": "fedex_api_key.api_key",
        "message": "What is your FedEx API key?"
      }
    ]
  }
}